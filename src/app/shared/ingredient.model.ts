export class Ingredient {
  constructor(public name: string, public amount: number, public unit: string) {}
  // it is a shortcut present in typescript to directly create and
  // initialize instance variable with parameters in constructor
}
